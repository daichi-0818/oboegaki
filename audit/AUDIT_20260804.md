# Pre-publication audit — 2026-08-04, revision 4 (local; no GitHub action taken)

Revision 4 supersedes earlier audits after three independent external
review rounds (13 findings, 5 reproduced unsafe cases, then cross-entry
link collisions — all addressed). All figures below are measured, not asserted;
the measurement commands are listed inline so a verifier can re-run them.

Method note: the concrete list of private identifiers grepped for is
deliberately **not** stored in this repository (storing it would embed the
identifiers). It is kept in the operator's session records. Every scan
covered tracked files, untracked files, ignored files, and full git
history (`git log -p`).

## Measured results

| Audit | Command | Result |
|---|---|---|
| Secret patterns (API keys, tokens, private key blocks, passwords) | pattern grep, all files + history | **0 hits** |
| Private identifiers (real names, employers, usernames, hostnames, internal project names, private absolute paths) | pattern grep, all files + history | **0 hits** in content; the commit author field carries the operator's public Git identity (open item 1) |
| Emails / URLs outside `example.com` and acknowledged repos | pattern grep | **0 hits** |
| Tracked files | `git ls-files \| wc -l` | 26 |
| Local commits at audit time | `git rev-list --count HEAD` | 5 (this revision ships in the same commit as hardening round 3) |
| Tests | `python3 -m unittest discover -s tests` | **55/55 PASS** |
| Fault-injection calibration | `tests/test_fault_injection.py` + calibration-style unit tests | covers: stale hash, broken md link, broken/ambiguous wiki link, orphan (incl. basename-shadow), exact duplicate (incl. cross-root), manifest tampering, unknown relation type, unresolved contradiction, missing source, dangerous/duplicate link targets, manifest escape (incl. via in-workspace symlink), case-alias of source (skipped on case-sensitive FS), backup-name collision class, planned-backup duplicates, backup-root overlap, mid-link failure rollback incl. replaced-symlink restoration and created-dir cleanup, cross-entry target=another-source equality/ancestor/descendant, portable-case duplicate targets |
| Zero-API enforcement | `tests/test_zero_api.py` | stdlib import whitelist + escape-hatch call scan (`os.system`/`popen`/`spawn*`/`exec*`/`fork`, `eval`, `exec`, `__import__`, `compile`) + socket-bombed dynamic run of the full CLI. Test-enforced, not a formal proof |
| Lint / syntax | `ruff check`, `python3 -m py_compile` | clean / OK |
| README quickstart | local steps re-executed on a fresh copy (the placeholder `git clone <this-repo>` line cannot run before publication) | PASS, and the intended FAIL demo reproduces. Verbatim end-to-end PASS from a fresh clone of the real URL is a mandatory publication-time checklist item |
| `memkit.py` size | `wc -l` | 863 lines, single file, stdlib only |
| Third-party code / fonts / images / datasets | manual review | none included |
| Idea-level inspirations | upstream license check (2026-08-02 / 2026-08-04) | two MIT projects acknowledged in README; no code copied |
| Sample data | manual review | 100% fictional (fictional bakery; `example.com` placeholders only) |

## Safety hardening in this revision

- `--manifest` is fully symlink-resolved (every existing component) before
  the workspace-containment decision; escapes — including via a symlink
  inside the workspace — fail before any write.
- `link` is two-phase: full preflight (dangerous targets `/`, `$HOME`,
  workspace root and their ancestors — including case-insensitive aliases
  via `samefile` for real paths; source/target overlap and samefile
  aliasing; nested and duplicate targets; backup directory overlapping
  any source/target; existing and planned backup collisions) rejects the
  entire run before a single write; every target is additionally checked
  against every entry's source (ancestor/descendant/samefile), and
  not-yet-created targets are deduplicated by an NFC+casefold portable
  key. Backup names are SHA-256 of the absolute target path
  (collision-resistant, plus preflight collision detection). Execution
  journals every mutation (moves, replaced-symlink original link text,
  created parent directories, created links) to
  `transaction_journal.json` and rolls all of it back on mid-failure;
  `restore_ledger.json` records full original/backup paths.
- Wiki links that resolve to more than one file fail as ambiguous.
- Orphan detection compares normalised relative paths, not basenames.
- Exact-duplicate detection pools across all opted-in memory roots.

## Open items for the publication decision (human)

1. **Commit author identity**: local commits carry the operator's normal
   public Git identity (name + public email). Standard for a personal
   portfolio repository; use `git commit --amend --reset-author` (or an
   interactive rebase) before pushing if a different identity is wanted.
2. **Repository name**: `agent-memory-kit` is already used on GitHub by
   two unrelated projects, one conceptually close. Recommendation: rename
   before publication (candidate: `oboegaki`).
3. **License copyright line** currently reads "agent-memory-kit
   contributors"; adjust if a personal name is preferred.
4. **Design-lineage publication rights (unconfirmed)**: this kit
   re-implements, in generic form, mechanisms first built in the
   operator's private tooling. Publication requires the operator to
   confirm they hold the rights to publish this design lineage (including
   any employer-side claim, if applicable). If that confirmation cannot
   be given, the fallback is a clean-room reimplementation in a separate
   environment from a written public specification only.
5. **Dedicated secret scanner**: gitleaks (or equivalent) is not installed
   in the build environment; per the pre-publication checklist it MUST be
   installed and run over the tree and full history immediately before
   publication.

## Verdict

Content is publication-clean by every measured audit above. Publication
remains blocked on the five human items and the explicit final approval
required by `docs/PRE_PUBLICATION_CHECKLIST.md`.
